using UnityEngine;

public class ToDo : MonoBehaviour
{
    // Not a script, but a conviently placed ToDo

    /*
    Add basic scripts for switches, buttons, walls, and other interactables
    Have them with publically editable strings for their names in the GameManager Dictionary
    Do not use set active, instead turn off the collisioin and visibility
    Make sure to call the SetFlag Function in GameManager in Enable

    Make script that tests Enable

    Morgan Section:

    Kill zones: Colliders that causes the player's death

    Kyle:
    Chekpoint object- detect when player touches it, send itself to the GameManager in a gameObject variable
    this variable will be referenced by the player character as where to go upon defeat.
    */
}
